// #include "Common.h"
// using namespace std;

// int copyContent(fstream infile, fstream outfile, int length){
//     char* temp = new char[length]{};//whatever store but never short than filename_len
//     infile.read((char *)temp, length);//read content in total size byte
//     outfile.write((const char *)temp, length);//write to target file
// }